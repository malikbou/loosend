<div class="navbar sticky-top navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
    <%= link_to toilets_path, class: "navbar-brand" do %>
      loo.send
    <% end %>
      <div class=“navbar-nav me-auto”>
        <% if user_signed_in? %>
          <div class="nav-item active">
           <%= link_to "Log Out", destroy_user_session_path, data: {turbo_method: :delete}, class: "log-button" %>
          </div>
        <% else %>
          <div class="nav-item">
            <%= link_to "Log In", new_user_session_path, class: "log-button" %>
          </div>
        <% end %>
      </div>
  </div>
</div>
